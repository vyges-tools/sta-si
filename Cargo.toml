[package]
name = "vyges-sta-si"
version = "0.0.1"
edition = "2021"
description = "Vyges tools"

# std-only (no external dependencies) so the core builds/tests offline.

[[bin]]
name = "vyges-sta-si"
path = "src/main.rs"

[lib]
name = "vyges_sta_si"
path = "src/lib.rs"
